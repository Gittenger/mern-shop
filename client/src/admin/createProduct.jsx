import React from "react";
import Layout from "../core/Layout";

const CreateProduct = () => (
	<Layout title="Create product" description="Create product page for Admin">
		<h2>Admin only route</h2>
	</Layout>
);

export default CreateProduct;
